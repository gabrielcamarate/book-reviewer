[project]
name = "review-cli"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "docx-adapter",
  "editorial-core",
]

[tool.uv.sources]
docx-adapter = { workspace = true }
editorial-core = { workspace = true }

[project.scripts]
review-cli = "review_cli.main:main"
